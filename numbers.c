#include <stdio.h>

// Messing around with number systems

void print_binary(int d) {
    // finish on wednesday
}

int main() {
    int bits = 0b1011101;
    printf("%d\n", bits);

    bits = 0b11101101;
    printf("%d\n", bits);
}